import { ScaleLoader } from "react-spinners";

const LoadingSpinner = () => (
  <div className="flex items-center justify-center py-12">
    <ScaleLoader color="#00fa19" />
  </div>
);

export default LoadingSpinner;
